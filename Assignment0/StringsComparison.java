package Assignment0;

public class StringsComparison {
	
	public static void main(String[] args) {
		
		// Declare two Strings
		// 1st String as 'name'
		// 2nd String as 'nickname'
		String name = "Shahul";
		String nickname = "Shaw";
		
		// Compare both Strings using if and else
		// Print 'name' and 'nickname' both are same" if it matches
		if (name==nickname) {
			System.out.println("name and nickname both are same");	
		}
		
		// Print 'name' and 'nickname' both are not same" if it doesn't match
		else {
			System.out.println("name and nickname both are not same");
		}
	}
}
